# The content language is fixed by design (2026-08-04): kinship terms render in Traditional
# Chinese, interface chrome in English. The former Languages list left together with the
# selector -- the capability was withdrawn, not defaulted.

StartingRole: self